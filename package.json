{
    "name": "@scope/multientry",
    "workspaces": [
        "packages/*"
    ],
    "version": "1.0.0",
    "private": true,
    "devDependencies": {
        "@types/jest": "24.0.22",
        "@types/node": "12.12.6",
        "@types/react": "16.9.11",
        "@types/react-dom": "16.9.4",
        "lerna": "^3.18.3",
        "react-app-rewired": "^2.1.5",
        "react-scripts": "3.2.0",
        "typescript": "3.7.2"
    },
    "scripts": {
        "start": "lerna run start --parallel",
        "start-pkg": "lerna run start --parallel --scope",
        "build": "lerna run build --parallel",
        "test": "lerna run test --parallel"
    }
}
